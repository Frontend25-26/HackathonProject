import { FC } from 'react';

import { StudentCourses } from '@/widgets/courses';

const StudentCoursesPage: FC = () => {
    return <StudentCourses />;
};

export default StudentCoursesPage;
