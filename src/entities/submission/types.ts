export interface DisplayedSubmission {
    id: string;
    student: string;
    course: string;
    hw: string;
    deadline: string;
    ciStatus: string;
    lastCommitDate: string;
    repositoryUrl: string;
}
